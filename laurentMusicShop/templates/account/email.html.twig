{% extends 'base.html.twig' %}

{% block title %}Modification de l'email{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modification de l'email</h1>
        {{form_start(myForm)}}
            {{form_widget(myForm)}}
                <button type="submit" class="btn btn-success">Confirmez la modification de l'email</button>
        {{form_end(myForm)}}        
    </div>
{% endblock %}